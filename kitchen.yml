driver:
  name: vagrant

provisioner:
  name: chef_infra

verifier:
  name: inspec

platforms:
  - name: centos-7

suites:
  - name: default
    run_list:
      - recipe[packagecloud_test::rpm]
      - recipe[packagecloud_test::rubygems_private]
